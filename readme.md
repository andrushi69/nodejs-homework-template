Create .env file while looking in .example.env, to see how it works. And then start checking:

**npm start**: starting server in production way
**npm-run-start:dev**: starting server in development way

